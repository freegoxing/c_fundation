//
// Created by freedom on 2024/9/1.
//

#include <stdio.h>

//int main()
//{
//    printf("%d", 23+24);
//    return 0;
//}

int main()
{
    printf("%d\n", 23+24);
    return 0;
}
//以printf为例： printf（“%[falgs][width][.prec ][hIL]type”）
//flag
//0：默认，不添加任何标志。
//+：正数前面添加正号，负数前面添加负号。
//-：左对齐，右边填充空格。
//

//%c:字符。
//%d:十进制整数。
//%i:整数，基本等同于%d。
//%f:小数（包含 float 类型和 double类型) 。
//%hd:十进制 short int型。
//%ho:八进制 short int型。
//%hx:十六进制 short int类型。
//%Lf: long double类型浮点。
//%0:八进制整数。
//%p:指针。
//%s:字符串。
//%u:符(unsigned int)。
//%x:十六进制整数。
//%%:输出一个百分号。



